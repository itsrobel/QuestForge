import * as React from 'react-native';
import { View, Text, Button } from 'react-native';

const TrackStory = ({ navigation }) => {
    return (
        <View>
        <Text>StorylineScreen </Text>
     
        </View>
    );
}

export default TrackStory;
