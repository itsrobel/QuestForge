import * as React from 'react-native';
import { View, Text, Button } from 'react-native';

const WorldMap = ({ navigation }) => {
    return (
        <View>
        <Text>World Map Screen</Text>
        {/* <Button
            title="Go to Home"
            onPress={() => navigation.navigate('Home')}
        /> */}
        </View>
    );
}

export default WorldMap;